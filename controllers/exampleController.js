import { JSONFilePreset } from "lowdb/node";

// Example controller structure

export function exampleAction(req, res) {
  res.json({
    message: "This is an example controller. Add your logic here!"
  });
}
